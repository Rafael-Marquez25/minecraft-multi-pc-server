[project]
name = "minecraft-multi-pc-server"
version = "0.1.0"
description = "CLI launcher for sharing a custom Minecraft server folder across multiple PCs."
requires-python = ">=3.11"

[project.scripts]
minecraft-launcher = "minecraft_multi_pc_server.cli:main"
